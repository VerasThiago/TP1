trait UIHandler {
    // prints universe
    def printWorld(cellses : Array[Cells], width : Int, height : Int) : Unit = {

    }

    // gives options and sends response
    def converse(options : List[String]) : Int = {

      0
    }
}
